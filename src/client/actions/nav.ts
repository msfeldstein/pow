import { File } from "../../types"

export function nav(file: File) {
    // if (file.type === 'comic') {
    //     window.location.href = `/view?file=${encodeURIComponent(file.fullPath)}`
    // } else if (file.type === 'book') {
    //     window.location.href = `/book?file=${encodeURIComponent(file.fullPath)}`
    // } else {
    //     setPath(file.fullPath)
    //     window.location.hash = newPath.join('/')
    // }
}